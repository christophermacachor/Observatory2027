{
  "name": "scalar-plasma",
  "version": "5.1.0",
  "description": "Scalar-Plasma Cycle with Cloudflare Pages Functions",
  "scripts": {
    "dev": "wrangler pages dev ./ --compatibility-date=2024-01-01",
    "deploy": "wrangler pages deploy ./ --project-name=scalar-plasma"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
